# Run this in your actual project (not this sandbox)

npm install @nestjs/swagger class-validator class-transformer

That's it — @nestjs/swagger bundles its own Swagger UI, no separate
swagger-ui-express install needed on recent NestJS versions.
